# CC-43: Decisão D1: o painel escrever `settings.json`

**Estado: bloqueado.** Precisa de decisão do Felipe antes de qualquer código.
**Não pegue esta task em sessão Sonnet sem essa decisão.**

---

## A pergunta que decide, e ela não é técnica

**O que um hook registrado pelo painel faz que um comando distribuído não faz?**

A resposta honesta é uma só: **obrigar em vez de pedir**. Se, com o CC-42 no ar,
os comandos já resolverem a dor, a D1 perde o motivo. E não fazer também é uma
decisão, melhor que construir por completude.

## O que muda do nível baixo para o alto

Hook roda **sem ninguém chamar**, disparado por evento. Para existir, precisa
estar registrado no `settings.json` do Claude Code. Dois riscos que escrever um
`.md` em `.claude/commands/` não tem:

- **JSON malformado desliga TODAS as configurações daquele arquivo, em
  silêncio.** Sem erro na tela. Os quatro hooks que hoje obrigam de verdade
  (`rota-guard`, `git-add-guard`, `cc check`, `routia`) parariam de valer sem
  aviso.
- **Hook errado roda a cada evento, em todo projeto**, sem o Felipe pedir.

O limite aceito hoje (CC-27) é explícito: o toggle **nunca** escreve no
`settings.json`. `cc hooks on|off <id>` só grava em `control-center.json`, e cada
hook checa `isEnabled()` sozinho. É por isso que a aba "hooks" tem badge
"registrado"/"não registrado" separado do liga/desliga.

## O plano, se for aprovado

Ordem pensada para que nenhum passo deixe a máquina pior que antes:

1. **Backup datado antes de qualquer escrita.** Mesmo padrão de `writeNotes`:
   `.bak` na gravação, cópia com data no apagamento total. `settings.json` é
   configuração digitada à mão, não tem outra fonte.
2. **Validar depois de escrever e reverter sozinho se quebrou.** Reler,
   `JSON.parse`, conferir que os hooks que existiam antes continuam lá. Falhou:
   restaura o `.bak` na hora e reporta.
3. **Escrita cirúrgica, nunca reescrita do arquivo inteiro.** Mexer só na chave
   do hook em questão. Vale a armadilha do `CONFIG_FILE`: restaurar arquivo
   inteiro de backup perde o que outro processo escreveu no meio-tempo.
4. **Um hook só, o menos perigoso, por uma semana** antes de abrir para os
   outros.
5. **Botão de pânico** visível na mesma aba: "devolver o `settings.json` ao
   estado anterior".

## Se a decisão for "não fazer"

Registre nos limites aceitos do ROADMAP com o motivo, e feche a task. Um limite
escrito vale mais que uma task aberta para sempre.
