# 2026-08-12 — cockpit commitado, e a agenda entra no painel (CC-20)

Sessão delegada por `/ceo`. Dois pedaços: fechar a dívida de commit do cockpit
de 10/08, e abrir a frente de conteúdo social pelo item que destrava o resto —
CC-20, calendário dentro do Control Center.

## O que entrou

**Cockpit** (commit `49f936c`, já pronto desde 10/08): config e abas do PiP,
fita de pastilhas, Docker local, VPS e processos pesados. Sem mudança de
código, só o commit que ficou pendente.

**Agenda** (CC-20): nova aba lê o Google Calendar pelo endereço secreto em
iCal — sem OAuth, sem chave de API, sem dependência nova.

- `src/calendario.mjs` — parser de ICS escrito do zero: desdobra linha longa
  (75 octetos, quebra com continuação em espaço), lê `DTSTART`/`DTEND` em três
  formatos (dia inteiro, UTC, hora local), expande `RRULE` (diário, semanal
  com `BYDAY`, mensal, anual, com `COUNT`/`UNTIL`/`EXDATE`), descarta evento
  cancelado. Teto de 500 ocorrências por evento, pra regra sem fim não virar
  loop.
- A URL do calendário é a credencial de leitura da agenda inteira: fica só no
  `control-center.json` da máquina, nunca volta pro navegador. `/api/calendario`
  devolve nome e eventos, jamais o endereço — nem em erro, nem em log.
- Tela nova: "hoje"/"amanhã" em vez de data crua, evento acontecendo agora
  destacado, o que já passou apaga mas continua visível (é a pergunta do
  digest de conteúdo: "o que eu fiz hoje").
- Escrita (criar evento por voz) fica pro CC-21, como o roadmap já previa.

## Bug achado rodando de verdade, não só `npm test`

A expansão de `RRULE` semanal perdia a última semana parcial da janela: o
corte olhava o cursor do dia, não o domingo da semana dele, e a semana que
começava depois do fim da janela mas tinha dias válidos dentro dela nunca era
gerada. Um evento "segunda a sexta" numa janela terminando na quarta perdia
segunda e terça da semana seguinte. Só apareceu testando contra um `.ics` de
mentira servido em localhost — os testes unitários (`test.mjs`) cobriam
`FREQ=WEEKLY` mas nenhum caso cruzava o fim de semana no meio da série.
Corrigido junto com `COUNT`, que contava repetição da regra em vez de
ocorrência (três vezes de "segunda e quarta" são duas segundas e uma quarta,
não três semanas inteiras).

## Armadilha nova

`CONFIG_FILE` (`~/.claude/control-center.json`) não é isolado por porta.
Subir instância de teste com `--port 8123` protege o processo do painel real,
não o dado — as duas leem e escrevem o mesmo arquivo. Um calendário de teste
foi parar no config de verdade do Felipe; removido cirurgicamente (só a chave
gravada pelo teste, sem restaurar o arquivo inteiro de um backup, porque o
daemon real escreveu atividade legítima no meio do teste). Detalhe em
`CLAUDE.md`.

## Estado

- `npm test` verde, com os casos de recorrência que pegaram o bug.
- Commits: `49f936c` (cockpit), `3b45a56` (docs da frente nova),
  `<a seguir>` (CC-20).
- CC-20 sai do ROADMAP como aberto. Próximo da frente: CC-21 (MCP do Google
  Calendar com escrita), que também é o que falta pra virar leitura+escrita.
