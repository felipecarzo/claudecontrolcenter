# 2026-08-21 — a sessão em que ele olhou a tela e apontou, uma coisa por vez

**Máquina:** VPS · **Sessão:** `c4e8a125` · **Rota:** `front`
**Modo:** contínuo · começou na noite de 20/08 e virou o dia

Nove apontamentos dele, todos vindos de olhar o painel no telefone. Cada um
virou um item fechado, e **cinco deles acharam defeito que o gate não tinha
como pegar**.

## O fio condutor do dia

O painel novo assumiu a raiz em 20/08. Ele herdou o código do antigo e **não
herdou as redes**: hoje isso apareceu três vezes seguidas, e cada uma custou um
defeito que só ele viu.

| o que o gate media | o que ele não media | como apareceu |
|---|---|---|
| largura de tela no `ui.html` | nada no `ui_v2.html` | três colunas espremidas em 390px |
| nada sobre estilo de módulo | classes do motor de gráficos | oito gráficos sem desenho |
| sintaxe do `ui.html` | sintaxe do `ui_v2.html` | página inteira parada em "Carregando..." |
| — | erro de execução, em qualquer lugar | tela Agora com bloco de 12 pixels |

Os quatro viraram verificação. A última exigiu navegador, e por isso está no
`npm run test:endereco`, não no gate.

## O que ele apontou, e o que estava por trás

**1. O PC desligado aparecia trabalhando (CC-218).** 14 sessões da máquina dele
congeladas, uma marcada como `working` e duas como `waiting`, 37 minutos depois
de o PC apagar. O dado estava certo desde o CC-184: `origem.semContato` vinha
carimbado em cada job. **A tela é que não lia o campo.**

**2. Não dava para saber onde cada sessão roda (CC-219).** Etiqueta por sessão,
nunca por projeto: o mesmo projeto roda nas duas máquinas ao mesmo tempo, e é
isso que ele quer distinguir.

**3. O cartão começava pela tarefa, não pelo projeto (CC-220).** *"o principal
em qualquer card é eu identificar qual é o projeto e aonde ele está"*. Inverti,
e a etiqueta de máquina passou a aparecer sempre no cartão do agente.

**4. O painel repetia o texto cru dele (CC-221).** Ele escolheu, entre três
caminhos, que o próprio agente escrevesse o resumo, e devolveu a pergunta que
decidia tudo: *"como vamos garantir que o agente sempre faça isso?"*.

A medição respondeu por que a instrução de 16/08 não garantia nada: **dos 16
agentes no painel, 2 tinham assunto escrito pelo agente.** Dez mostravam o texto
dele (incluindo "comite", "comita" e um caminho de arquivo do Windows), quatro
não tinham nada. Agora o assunto tem hora, a cobrança recusa eco do pedido dele
e assunto mais velho que o último pedido, e a tela marca o que não foi resumido.

**5. A tela Trabalho quebrada no telefone (CC-222).** Grade de três colunas em
`style=` inline, a armadilha que já estava escrita no `CLAUDE.md` desde 16/08 e
voltou num bloco novo. Ao ligar a rede, ela pegou **mais três telas** que
ninguém tinha visto.

**6. Cada tela ganhou endereço (CC-223).** Voltar volta, recarregar mantém.
`#hash` e não caminho, porque o painel é servido em três lugares e o hash nunca
chega ao servidor. Nove casos provados em Chrome de verdade.

**7. Os gráficos não desenhavam (CC-224).** E **não era coisa de telefone**: o
CSS do motor nunca foi copiado para o painel novo, então as barras nasciam com
altura zero. A rosca aparecia porque é a única forma que carrega o próprio
tamanho, e foi ela que fez o defeito parecer de celular.

**8. "Por que a primeira coisa que precisa de mim é o meu PC desligado?"
(CC-226).** Pergunta dele, correção de um julgamento meu de poucas horas antes.
O painel não sabe distinguir "desligou" de "caiu", então não pode chamar aquilo
de urgência.

**9. A regra da máquina não estava em todo lugar (CC-227).** Segunda vez que ele
pedia. Na primeira eu tinha enfraquecido com duas economias inventadas por mim.
Saíram as duas, e a trava que ele pediu junto entrou.

## As duas rodadas do "?"

**Primeira (CC-228):** o "?" ao lado dos termos, lendo do glossário que já
varria o `docs/`. Achei dois defeitos calados no caminho: um documento inteiro
mudo há dias por um hífen no lugar de dois pontos, e um `catch` que apagava os
95 termos já carregados.

**Ele leu e reprovou:** *"voce colocou os '?' bem ruinzinhos (…) voce falou como
se eu fosse um imbecil"*. E escreveu o modelo do que queria.

**Segunda (CC-229 e CC-230):** o formato tinha que mudar antes do texto, porque
uma linha não comporta explicar o bloco inteiro da tela. Agora cada `##` de
`docs/produto/PALAVRAS-DA-TELA.md` é uma explicação, e são **50**: 26 de termo e
24 de tela, uma para cada tela do menu.

A ligação é por derivação (`view-agentes` procura `tela: agentes`), então não
existe segunda lista para manter, e tela nova sem explicação vira falha no gate.

## Quatro erros meus, no mesmo dia

Registrados porque o padrão importa mais que cada um:

1. **Quebrei a página inteira** com uma crase dentro de comentário HTML dentro
   de template literal. Nada rodou, e não havia erro visível.
2. **Usei variável fora de escopo** na rota de pendências, que passou a derrubar
   a conexão.
3. **Removi uma variável e deixei o uso dela** (CC-231), travando o desenho da
   tela Agora no meio. **Foi ele quem achou, num print.**
4. **Quase reportei um defeito que não existia**: a ferramenta de captura
   desenha caixa deslizante fora do lugar, e a tela parecia abrir na terceira
   coluna com a rolagem medida em zero.

Os três primeiros viraram verificação. O quarto virou armadilha escrita.

## O que foi medido hoje, e vale guardar

- 2 de 16 agentes escreviam o próprio assunto
- 22 rotinas desatualizadas em 5 projetos (medição anterior, agora explicada na tela)
- o cartão de projeto ficava com 310px numa tela de 390, por 24px de recheio de cada lado
- 15 das 21 grades do painel novo já eram responsivas por construção; 3 não eram
- 95 termos no glossário, contra 65 no começo do dia

## O que fica para ele

1. **Conferir os nove apontamentos** no telefone. Ele disse "já já confiro" e
   ainda não voltou em cima dos primeiros.
2. **Apagar o `ui.html`** continua pendente desde 20/08.
3. **Os nomes dos papéis**, também de 20/08.
4. **A pasta `tools/` está com dono errado na VPS** (`nobody`), então nada ali
   pode ser editado sem senha de administrador. Contornei com cópia no
   scratchpad, mas o conserto exige ele.
