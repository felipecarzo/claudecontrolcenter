# 2026-08-20

Sessão `5a0496cf`, rota `front`, no PC. O dia em que o backlog zerou: 44
itens fechados, do CC-168 ao CC-213.

O pedido dele foi curto duas vezes, e as duas mudaram a escala do dia:
*"o backlog ta registrado pra iterarmos? se sim pode comecar"* de manhã, e
*"vamos zerar o backlog"* no fim.

## O que era isto, antes de começar

O painel novo (`/v2`) tinha a cara certa e pouca coisa por dentro: **8 de 24
destinos do menu entregavam conteúdo; 16 abriam um esqueleto** que dizia
"conecte a API respectiva neste módulo", com uma bolinha verde e a palavra
"Operante" ao lado. A maquete tinha sido feita para parecer pronta.

No fim do dia são 23 destinos, todos com dado real, varridos um a um no PC e
na VPS, sem um erro de console.

## As telas que passaram a existir (CC-195 a CC-203)

Nesta ordem, cada uma provada antes da seguinte:

| tela | o que ela responde |
|---|---|
| o que mudou desde que olhei | o que aconteceu enquanto ele estava fora |
| remoto e federação | as máquinas ligadas, e abrir sessão à distância |
| estrutura | o mapa de um projeto: frentes, rotas e o que mudou no git |
| notas | o único conteúdo do painel sem outra fonte |
| framework, glossário, VPS | as três que mostravam dado inventado |
| servidores, docker, máquina, hooks, rotinas | a infraestrutura |
| custo, gráficos, digest | o que os números dizem quando cruzados |
| documentos, bancada, agenda | a referência |

Duas decisões que valem mais que o código:

**O motor de gráficos não foi reescrito.** As 352 linhas do `graficos.js`
passaram a rodar dentro do painel novo como estavam, e os oito gráficos
prontos desenharam com dado real na primeira tentativa.

**A entrevista não virou tela.** Ela mora dentro do Framework, embaixo do
projeto a que pertence, que era a decisão do CC-133 e continua certa.

## A limpeza do menu (CC-203), e o que ela revelou

Cinco destinos saíram: **ideias**, **dependências** e **histórico** não têm
rota nenhuma no servidor (eram nome no menu e nada atrás), e **rotas** e
**fila** duplicavam o que já mora em Estrutura e em Agora.

Junto saiu o gerador de maquete, que era o pior dos dois: qualquer destino
sem tela ganhava um cartão com bolinha verde escrito "Operante". Hoje ele diz
que a tela não existe e chama isso de defeito.

## Sete defeitos que faziam a tela mentir (CC-204 a CC-210)

Ele escolheu esta frente sobre o desenho mobile, e a escolha estava certa:
os sete eram a mesma família.

- **Clique que falha por rede agora aparece.** `post()` devolvia `undefined`
  no erro, e quem chamava fazia `if (!out?.ok) return`, que é o mesmo caminho
  de uma resposta boa. Botão piscava, nada acontecia, e ele concluía que o
  recurso não existia. Já tinha acontecido duas vezes, registrado no CC-158.
- **A tela parou de fugir do dedo.** `pintar()` não escreve quando o HTML é
  idêntico e não escreve quando o cursor dele está dentro. Provado marcando
  nós do DOM e conferindo que sobrevivem a quatro tiques, mais a prova
  negativa: desligando a guarda, todos morrem de novo.
- **O gráfico parou de ignorar o filtro de aparelho calado.** Não dá para
  aplicar (a medição por máquina existe no total, não no dia a dia), e agora
  a tela diz isso com a razão.
- **"Horas por dia" parou de inflar** com duas máquinas: virou faixa
  (`10 a 13d`) em vez de número exato falso.
- **Hora herdada ganhou prazo de 12h.** Preservar o que o pacote não traz
  consertou o defeito de 19/08 e criou o irmão: se a varredura quebrar do
  outro lado, a tela mostraria hora de anteontem em verde para sempre.
- **Rede caída não custa mais dez minutos de dado**, e o arquivo entre
  máquinas ganhou teto próprio, medido no que vai para o disco.

## O celular (CC-181), em duas partes

A primeira veio de um clique dele: *"em cockpit > agora, em um agente ao
clicar abrir agente nao vai p lugar nenhum"*. O botão copiava
`claude --resume <id>` e piscava "copiado ✓". Prometia navegação e fazia
outra coisa.

Testando o conserto em 390px, apareceu o defeito grande: **a tela de agentes
dava 26px de conteúdo dentro de 358 disponíveis**, uma coluna de texto de uma
letra de largura. A coluna lateral de 300px estava no `style=` do elemento, e
estilo inline vence media query. Duas telas estavam assim.

Depois disso o desenho de verdade: barra de baixo com quatro alvos e a
gaveta, e os cartões deslizantes. **A gaveta é clone da barra lateral, não
uma segunda lista**: tela nova aparece nos dois lugares sem ninguém somar.

Medido em 390 de verdade, com a régua barata deste projeto: os cinco botões
em 39/117/195/273/351, passo 78, que só fecha com 390.

## O botão do escritório (CC-191): três defeitos empilhados

Respondia `spawn EFTYPE` e nada subia.

1. O fork é um `cli.js`, e no Windows `.js` não é executável. A saída antiga,
   `shell: true`, além de não resolver era injeção de comando com argumento
   dinâmico.
2. A rota respondia `ok` antes de o erro do `spawn` chegar (ele vem num
   evento, não no `try`).
3. Depois de subir, a tela relia um cache de 15s e seguia dizendo "está
   parado" com o processo de pé.

**E aqui eu caí na armadilha que está escrita no CLAUDE.md deste projeto:**
`Stop-Process` falhava calado e havia **sete processos acumulados na porta
8134**, todos anteriores ao código novo. Testei cinco vezes contra código
velho antes de olhar a hora de criação deles. A armadilha estava escrita,
com o comando certo, e eu não conferi.

## Os painéis dele (CC-177/178/179)

Pedido dele de 19/08, com as palavras dele: *"uma janela de favoritos onde eu
mesmo adiciono painéis diferentes de todas as seções"*.

A decisão que barateia tudo: o painel dele **copia o bloco pronto** a cada
tique, em vez de redesenhar por conta própria. Parece preguiça e é o
contrário: conserto num bloco aparece lá de graça, e é impossível a tela
discordar da origem porque não existe segunda conta. O preço, pago: o clone
perde os `id`, senão `getElementById` passaria a devolver o clone.

O teto de nove painéis não é técnico, é a tecla: a troca é pelas teclas 1 a
9, e um décimo não teria como ser chamado.

## A troca (CC-176 / CC-186)

`cockpit.carzo.com.br` serve o painel novo, no PC e na VPS. **O antigo não
foi apagado**: continua inteiro em `/v1`, e voltar atrás é trocar duas linhas
em `web.mjs`. Apagar o `ui.html` é a única parte irreversível, e fica com
ele.

## O que mais fechou

- **`cc federar`** (CC-187): uma máquina nova entra sozinha, sem SSH. Grava e
  **confirma mandando um pacote de verdade na hora** — provado nos dois
  desfechos contra a VPS real, token certo e token errado.
- **A linha de tendência voltou** (CC-189), agora medida: some com menos de
  três dias de consumo, e compara três dias contra três, nunca ontem contra
  hoje (um fim de semana viraria "caiu 80%").
- **Aparelho mudo aparece** dizendo "não reportou" em vez de sumir e encolher
  o total sem uma palavra (CC-185/188).
- **O tamanho de uma tarefa** entrou no `AGENTS.md` (CC-180): cabe numa linha
  do painel com um resultado conferível, ou é grande demais.

## Método

Cada item foi provado antes do seguinte, e a prova é sempre a mesma forma:
subir o painel de verdade, dirigir pelo navegador sem interface gráfica,
conferir o texto que aparece na tela e o console vazio, no PC **e** na VPS.
Nenhum item foi dado como feito por o código parecer certo.

Duas vezes a prova achou defeito que a leitura não acharia: os 26px do
celular e o cache de 15 segundos do escritório.

## A revisão dele, depois do backlog zerado

Com o backlog em zero, ele definiu o ritmo: *"eu falo uma, você verifica, a
gente resolve, e depois eu volto e verifico outra (…) como está praticamente
pronto, agora é mais uma questão de eu olhar primeiro pedaços e revisar o todo
depois"*.

**Quatro defeitos saíram daí, e nenhum deles o gate pegaria.** Todos só
aparecem para quem usa a tela. Vale registrar como categoria: backlog zerado
não quer dizer produto pronto, quer dizer que acabou o que alguém já tinha
escrito.

### CC-214: clicar num agente mostrava todos os agentes

Palavras dele: *"não faz sentido clicar em ver o agente e eu não vejo aquele
agente, eu vejo todos os agentes e as últimas tarefas de cada agente"*.

A tela tinha lista mais uma coluna de detalhe de 300px ao lado. O que ele foi
ver ganhava 300px; o resto da tela repetia a lista que ele acabara de deixar.

Agora clicar abre a tela dele: a lista some e volta pelo botão. Nela, todas as
tarefas separadas em "falta fazer" e "já fez", **com a hora em que cada uma
fechou** (o dado já existia em `feitoEm` e nenhuma tela mostrava), todos os
bloqueios em vez de só o primeiro, e o último pedido inteiro.

Um defeito menor junto: clicar duas vezes no mesmo agente fechava, o que fazia
o segundo clique parecer que a tela travou.

### CC-215: o painel mostrava recado do programa como pedido dele

*"me explique essa zona de ultimos pedidos que ele mostra. e estao em ingles,
porque? colocar em portugues é custoso?"*

**Não era idioma, e traduzir seria o erro.** O CLI grava as mensagens dele e as
próprias no mesmo arquivo. O resumo que ele injeta quando o contexto estoura
começa com "This session is being continued from a previous conversation", e
passava pelo filtro, que não conhecia o marcador `isCompactSummary`.

Sessão longa é justamente a que mais aparece no painel: **o cartão mais
importante era o que mais mentia** sobre o que ele tinha pedido.

Três achados no caminho: `/compact` sozinho ocupava o lugar do pedido de
verdade, `[Request interrupted by user]` às vezes chega sem o marcador de
interrupção, e "não consegui ler" virou frase diferente de "ninguém escreveu
nada nesta sessão" (eram o mesmo `null`, e um deles é falha).

### CC-216: a pergunta que valeu mais que o conserto

*"tudo bem que era mentira, mas como garantimos que isso nao vai acontecer
mais?"*

A resposta honesta começa admitindo: **o filtro não garante nada**. Ele é uma
lista do que se conhece, e o CLI ganha campo novo a cada versão.

Tentei inverter para lista de inclusão (aceitar só o que traz `promptSource`).
**Medi antes, e estava errado**: nos 266 transcritos da máquina, 37 das 2253
mensagens aceitas não trazem a marca, e algumas são dele de verdade. Inverter
perderia pedido real.

O que garante é uma varredura no gate contra os transcritos REAIS: lê os 60
mais recentes, olha o que o painel mostraria, e falha com o nome do arquivo
quando tem cheiro de recado de máquina. **Provei nos dois sentidos**:
reintroduzi o defeito de propósito e o teste falhou apontando o projeto;
restaurei e voltou a passar.

### CC-217: a trava cobrando trabalho que não existia

*"o guarda ve coisas abertas de resposta anterior… mas e ai? precisamos
resolve-las? ou nao?"*

Não precisava: **nenhuma das sete era tarefa.** Eram títulos do texto que
explica a frente ("O que falta", "A ordem de migração, por valor para ele"), e
a trava contava qualquer `###` sem marca de concluído.

Item de backlog tem código, e agora ela exige isso. O custo do defeito é maior
que o barulho: **cobrança falsa ensina a ignorar a trava**, que é o oposto do
que ela existe para fazer.

## A migração para a VPS, e o risco que ela revelou

*"podemos micrar o projeto pra vps tranquilamente e programar de la sem quebrar
o projeto?"*

Medi antes de responder, e o risco não era a VPS: **o painel novo inteiro, 322
KB, nunca tinha sido commitado.** Ele existia só como arquivo solto em duas
máquinas, porque eu copiei por SSH o dia todo. Uma pasta perdida e o dia
acabava.

As duas estavam no mesmo commit e as duas com trabalho fora do git. Comparei
arquivo a arquivo: o código era **idêntico** nas duas, só dois documentos
divergiam.

Resolvido em seis commits separados por assunto, enviados ao GitHub e puxados
na VPS. Antes do pull, conferi por assinatura que o arquivo local da VPS era
igual ao que vinha do git, para nada ser sobrescrito às cegas.

Ficaram de fora, de propósito: rascunhos e sondagens (o repositório é público,
e os arquivos de sondagem da VPS carregavam saída crua com caminho de
credencial), as 67 capturas de tela, e quatro arquivos que são de outra sessão.

**Um deles exigiu cuidado:** `src/ui.html` tem 63 blocos de mudança e só 1 era
meu. Apliquei só aquele bloco ao commit, deixando as 864 linhas da outra sessão
intactas.

## O que fica para amanhã

Nada no backlog. As duas coisas que dependem dele:

1. **Rodar o painel novo alguns dias** e dizer se o antigo pode ser apagado.
2. **Abrir no celular** e dizer se está bom de usar. As capturas dizem que
   sim; o teste que vale é o dele.
